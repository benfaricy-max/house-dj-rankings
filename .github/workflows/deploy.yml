name: Deploy Frontend

on:
  push:
    branches: [main]
  workflow_run:
    workflows: ["Refresh Rankings"]
    types: [completed]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    # Don't redeploy stale data when a refresh run failed/timed out; a push always deploys.
    if: ${{ github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success' }}
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install and build frontend
        # Commerce build vars come from repo Variables (Settings → Secrets and
        # variables → Actions → Variables). Until they're set they're empty, so
        # checkoutReady is false and the live site is unchanged. Set both to go
        # live: VITE_API_BASE=<your deployed api URL>, VITE_PAYWALL_ENABLED=true.
        env:
          VITE_API_BASE: ${{ vars.VITE_API_BASE }}
          VITE_PAYWALL_ENABLED: ${{ vars.VITE_PAYWALL_ENABLED }}
        run: |
          cd frontend
          npm ci
          npm run build

      - uses: actions/configure-pages@v4

      - uses: actions/upload-pages-artifact@v3
        with:
          path: frontend/dist

      - uses: actions/deploy-pages@v4
        id: deployment
