# thedjrankings.com (PEAKTIME)
User-agent: *
Allow: /

# Private / non-indexable surfaces
Disallow: /pitch/
Disallow: /journal

# Sitemap — live as of the Phase B prerender migration. Built by
# backend/generatePages.js (lists only prerendered /artist & /value pages + home,
# all 200). markets/clubs/blog follow in Phase B.2 (backend/generateSitemap.js).
Sitemap: https://thedjrankings.com/sitemap.xml
